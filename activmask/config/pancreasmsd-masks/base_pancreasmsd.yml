cuda: True
num_epochs: 500
schedule_steps: 10
batch_size: 32
shuffle: True

# Dataset
dataset:
  train:
    PancreasMSDDataset:
      mode: 'train'
      blur: 16
      nsamples: 128
  valid:
    PancreasMSDDataset:
      mode: 'valid'
      nsamples: 256
  test:
    PancreasMSDDataset:
      mode: 'test'
      nsamples: 256

# Optimizer
optimizer:
  Adam:
    lr: 0.0001

transform:
        train: {}
        valid: {}
        test: {}
